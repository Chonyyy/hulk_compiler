let a: Number = 42 in if (a % 2 == 0) print("Even") else print("odd");
let a: Number = 42 in print(if (a % 2 == 0) "even" else "odd");
let a: Number = 42 in
    if (a % 2 == 0) {
        print(a);
        print("Even");
    }
    else print("Odd");
let a: Number = 42, let mod: Number = a % 3 in
    print(
        if (mod == 0) "Magic"
        elif (mod % 3 == 1) "Woke"
        else "Dumb"
    );
