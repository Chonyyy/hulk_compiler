function gcd(a: Number, b: Number): Number => while (a > 0)
    let m: Number = a % b in {
        b := a;
        a := m;
    };
for (x: Number in range(0, 10)) print(x);
{let iterable: Iterable = range(0, 10) in
    while (iterable.next())
        let x: Number = iterable.current() in
            print(x);
let a: Number = 10 in while (a >= 0) {
    print(a);
    a := a - 1;
}}