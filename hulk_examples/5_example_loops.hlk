function gcd(a, b) => while (a > 0)
    let m = a % b in {
        b := a;
        a := m;
        if (a == 0) b else a;
    }

{
    let a = 10 in while (a >= 0) {
        print(a);
        a := a - 1;
    }
    print(gcd(5, 10));
}