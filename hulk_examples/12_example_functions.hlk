function tan(x: Number): Number => sin(x: Number): Number / cos(x: Number): Number;
function cot(x: Number): Number => 1 / tan(x: Number): Number;
function tan(x: Number): Number => sin(x: Number): Number / cos(x: Number): Number;

print(tan(PI: Number): Number ** 2 + cot(PI: Number): Number ** 2);

function operate(x: Number, y: Number) {
    print(x + y);
    print(x - y);
    print(x * y);
    print(x / y);
}
