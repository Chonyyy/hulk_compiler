let numbers: Vector = [1,2,3,4,5,6,7,8,9] in
    for (x: Number in numbers)
        print(x);
let numbers: Vector = [1,2,3,4,5,6,7,8,9] in print(numbers[7]);
let squares: Vector = [x^2 || x: Number in range(1,10)] in print(x);